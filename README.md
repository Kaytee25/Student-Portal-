NUST Student Portal — Login Mockup

This folder contains a small static mockup of the student portal login page based on the screenshot you provided.

Files:
- index.html — the login page
- styles.css — styles for the page

To view locally:

1. Open `index.html` in your browser (double-click or use "Open File").

Optional development steps:

- Serve with a static server (Node.js) for local testing:

```bash
# using Python 3
python -m http.server 8000
# then open http://localhost:8000
```

Next steps I can do for you:
- Replace the static form with real authentication (JWT/session).
- Integrate your provided images and exact logo.
- Scaffold a full React frontend and API backend.

Adding the crest image:
- Save the crest image you uploaded as `images/nust-crest.png` inside this folder. The left panel will display a blurred version automatically.
- If you want a different blur amount or placement, tell me and I'll adjust `styles.css`.

Tell me which of the next steps you'd like, or upload additional screenshots for other pages.
